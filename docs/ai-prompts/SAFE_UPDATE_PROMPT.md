# Step 27 — Safe Update Prompt

```text
Please update only the required files for this change.

Important:
- Do not rewrite unrelated files
- Do not change existing behavior unless necessary
- Follow the existing project structure
- Follow existing naming conventions
- Keep page.tsx small and import main feature component only
- Keep reusable UI inside features/[module]/components
- Keep dialogs only for small actions
- Keep backend controller/service/repository separated
- Explain which files you changed and why
```
