# Step 22 — PWA Setup Prompt

```text
Using the Father Business Management System context, set up PWA support.

Goal:
Make the web system installable on mobile and feel like an app when opened from the home screen.

Requirements:
1. Add web app manifest
2. Add app name: Father Business
3. Add short name: Workshop
4. Add app icons
5. Add theme color
6. Add background color
7. Set display mode to standalone
8. Add proper viewport setting
9. Add mobile safe-area support
10. Add basic offline fallback page if possible
11. Cache basic static assets if suitable
12. Make sure it works on Android Chrome and iPhone Safari

PWA UX:
- When installed, the app should open without looking like a normal browser website
- Mobile layout should feel like an app
- Use app header and bottom navigation for mobile
- Avoid desktop-only sidebar on mobile
- Make all main actions touch-friendly

Before coding:
1. Tell me which PWA package or approach you recommend and why
2. List files to create/update
3. Then implement step by step
```
